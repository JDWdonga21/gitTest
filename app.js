const calculator = {
  add: function (a, b) {
    console.log(a + b);
  },
  sub: function (a, b) {
    console.log(a - b);
  },
  mult: function (a, b) {
    console.log(a * b);
  },
  div: function (a, b) {
    console.log(a / b);
  },
};

calculator.add(10, 2);
calculator.sub(10, 2);
calculator.mult(10, 2);
calculator.div(10, 2);
